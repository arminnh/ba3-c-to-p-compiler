#include <stdio.h>

int main(void)
{
    int a, d, *f;
    float b, c, e;

    b;
    1;
    b + 1.0;
    a = d;
    b = 1.0 * 2.0;
    c = (b - 0.5) + 1.0;
    'c';
    // a = 5 + 5 + a + (c);
    // d = 5 * (5 + 5 + d) + e;
    5.5;
    // e = (5 * (5 + 5 + d) + e) * 7 - 5;
    // e = ((5 * (5 + 5 + d) + e) * 7 - 5) / 100;
    2 % 1 + 2 * 5 - 3 / 4; // 10

    1 == 2;
    1 <= 2;
    1 != 2;
    // 3 == 3.0 == 3;
    3 == 3 == 3;

    // printf("Hello World \n");
    // printf("%d, %f, %d, %c, %f\n", 23, 45.0, a, 'a', 5 * (5 + 5 + d) + e);

    // a ? b : c;
    // a = (1 == 2) ? 0 : 1 ;
    // d = (1 == 1) ? 0 + 3 * 8 : 1 + 3 ;

    // if (1 && a) a++;
    // if (1 || a) a--;

    // a || b && !c || !(d && e);
    // a = b;

    a++;
    --a;
    ++a;
    a--;

    &a;
    *f;
    // !a && !b;
    // !(a || b);
    // !a && !d;
    // !(a || d);

    return 1;
}