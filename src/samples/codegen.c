int main(void) {
    int a;

    a = 2;
    a + 1;
    a = a + 6;

    // a = a + 1;
    // -4;
    // +4;
    // 4 ==5;
    // a != 5;
    // 8 < 9 && 9 < 8;
    // 9 > 9 || 9 < 9;
    // ++a;
    // --a;
    // 3 * 8;
    // 7 / 3;
    // 4 - 5;
    // 3 * (4 + 5) / 7 * a;
    // a = (a = (a = 1));
}
