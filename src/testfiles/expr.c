int main(void)
{
    3 < 2;
    3 == 2;
    5 - 3;
    3 == 3.0 == 3;
    2 % 1 + 2 * 5 - 3 / 4;

    a ? b : c;

    a || b && !c || !(d && e);
    a = b;

    a++;
    --a;
    ++a;
    a--;

    &a;
    *a;
    !a && !b;
    !(a || b);

    // return 0;
}
