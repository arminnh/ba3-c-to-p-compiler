int main(void)
{
    int a, d, *f, x;
    float b, c, e;

    b;
    1;
    b + 1.0;
    a = d;
    b = 1.0 * 2.0;
    c = (b - 0.5) + 1.0;
    'c';
    a = 5 + 5 + a + (x);
    d = 5 * (5 + 5 + d) + x;
    5.5;
    e = (5.5 * (5.7 + 5.4 + c) + e) * 7.0 - 5.2;
    e = ((5.0 * (5.5 + 5.6 + c) + e) * 7.7 - 5.8) / 100.0;
    x = 2 % 1 + 2 * 5 - 3 / 4;

    1 == 2;
    1 <= 2;
    1 != 2;
    3 == 3 == 3;

    a ? b : c;
    a = (1 == 2) ? 0 : 1 ;
    d = (1 == 1) ? 0 + 3 * 8 : 1 + 3 ;

    if (1 && a) a++;
    if (1 || a) a--;

    a || d && !x || !(d && x);

    a++;
    --a;
    ++a;
    a--;

    &a;
    *f;
    !a && !x;
    !(a || x);
    !a && !x;
    !(a || d);

    return 1;
}
